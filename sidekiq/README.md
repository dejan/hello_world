# Sidekiq (standalone) quickstart

"HelloWorld" app with standalone Sidekiq.

## Start

    docker-compose up

This will run the worker and the client that will put a job every 3 seconds. Access sidekiq UI at: http://localhost:9292